CREATE TABLE person (
                        person_id serial PRIMARY KEY,
                        firstname VARCHAR ( 50 ) NOT NULL,
                        secondname VARCHAR ( 50 ),
                        lastname VARCHAR ( 50 )
);
