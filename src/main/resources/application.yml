#spring:
#  datasource:
#    flyway.url: jdbc:postgresql://localhost:5432/address-book-db
#    flyway.username: admin
#    flyway.password: admin
#    flyway.defaultSchema: address-book
#    flyway.driver-class-name: org.postgresql.Driver
#
#spring.jpa.hibernate.ddl-auto: update
#spring.flyway.baseline-on-migrate: true


spring:
  application:
    name: address-book
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/address-book-db
    password: admin
    username: admin
    testOnBorrow: true
    testWhileIdle: true
  flyway:
    defaultSchema: address-book
    baseline-on-migrate: true



#    url: jdbc:postgresql://localhost:5432/address-book-db?currentSchema=AddressBook